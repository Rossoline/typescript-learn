// Block 15 — barrel (public API)
// TODO: re-export the public API so consumers can import everything from here:
//   export { add, multiply } from "./math";
//   export { formatMoney } from "./format";
//   export type { Money } from "./types";
export {};
